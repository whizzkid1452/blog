---
title: 'First Post'
description: 'An introduction to the blog structure that separates Markdown posts, routes, and shared code.'
---

## Why this structure exists

This post lives in `content/posts`, while routing stays in `app` and shared code stays in `components` or `lib`.

## How to add the next post

Create another `.md` file in `content/posts`. The file name becomes the URL slug.
